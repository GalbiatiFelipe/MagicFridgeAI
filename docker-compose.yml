version: '3.8'
services:
  postgres:
    image: postgres:latest
    container_name: magicfridgeaicontainer
    environment:
      POSTGRES_DB: magicfridgeai
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"
    restart: always